apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  labels:
    app: mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: openemr-secrets
              key: mysql-root-password
        - name: MYSQL_USER
          value: openemr
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: openemr-secrets
              key: mysql-password
        - name: MYSQL_DATABASE
          value: openemr
        volumeMounts:
        - name: mysql-data
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-data
        emptyDir: {}
